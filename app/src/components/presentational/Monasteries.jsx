import React from 'react';
import {Container} from 'react-bootstrap';

class Monasteries extends React.Component {
    render() {
        return(
            <div>This is the Monasteries Page</div>
        )
    }
}

export default Monasteries;